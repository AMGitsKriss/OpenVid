@model OpenVid.Areas.Playback.Models.VideoList.VideoListViewModel
<div class="newPage">
    <div>
        @{await Html.RenderPartialAsync("_Pagination", Model); }
    </div>

    <div class="row">
        @foreach (var video in Model.Videos)
        {
            await Html.RenderPartialAsync("_Thumbnail", video);
        }
    </div>

    <hr />


    <div>
        @{await Html.RenderPartialAsync("_Pagination", Model); }
    </div>
</div>

<script>
    window.scrollTo(0, 0);
</script>